# دليل الإعداد والاستخدام - تطبيق جدول الضرب

## ✨ الميزات الجديدة المضافة

### 1. 📝 التسجيل المحسّن
- ✅ **الاسم الثلاثي إلزامي**: يجب إدخال الاسم الأول والأوسط والأخير
- ✅ **بيانات الطالب**: المدرسة، الصف (1-6)، الفصل (1-10)
- ✅ **رسالة نجاح**: عرض رسالة تأكيد بعد التسجيل بنجاح
- ✅ **التحقق من الأخطاء**: معالجة الأخطاء وعرضها بشكل واضح

### 2. 🔐 تسجيل الدخول بـ Google
- ✅ **طلب البيانات الإضافية للطلاب**: عند تسجيل الدخول بـ Google كطالب، سيُطلب منك إدخال:
  - اسم المدرسة
  - الصف (1-6)
  - الفصل (1-10)
- ✅ **إصلاح مشاكل التوافق**: تحديث للنسخة 7.x من google_sign_in

### 3. 📧 إعادة تعيين كلمة المرور
- ✅ **إرسال إيميل تلقائي**: عند الضغط على "نسيت كلمة المرور" يتم إرسال رابط إعادة التعيين إلى بريدك
- ✅ **رسائل واضحة**: عرض رسالة تأكيد أو خطأ حسب الحالة

### 4. 📊 تصدير بيانات الطلاب كـ Excel
- ✅ **تصدير شامل**: المعلم يمكنه تصدير بيانات جميع الطلاب كملف Excel
- ✅ **البيانات المصدرة**:
  - الرقم، الاسم الكامل، البريد الإلكتروني
  - المدرسة، الصف، الفصل
  - المجموع الكلي، عدد الاختبارات المكتملة
  - تاريخ الإنشاء
- ✅ **ورقة ملخص**: ملخص إحصائي يشمل:
  - إجمالي عدد الطلاب
  - متوسط النقاط
- ✅ **حفظ تلقائي**: يتم حفظ الملف في مجلد التنزيلات

### 5. 👤 عرض بيانات الطلاب
- ✅ **للمعلم**: عرض كل طالب بجميع بياناته:
  - الاسم، البريد الإلكتروني
  - المدرسة، الصف، الفصل
  - النقاط والإحصائيات
- ✅ **واجهة محسّنة**: تصميم جميل وسهل القراءة

---

## 🔧 الإعداد المطلوب

### 1. تثبيت المكتبات
```bash
flutter pub get
```

### 2. إعداد Firebase

#### أ. تكوين Firebase للمشروع
1. تأكد من إضافة ملف `google-services.json` في: `android/app/`
2. تحديث `firebase_options.dart` ببيانات مشروعك:
   - ✅ Android: تم إضافة البيانات
   - ⚠️ Web: قم بإضافة Web App في Firebase Console وتحديث `appId`

#### ب. إضافة SHA-1 Fingerprint
قم بتشغيل الأمر التالي:
```powershell
keytool -list -v -keystore "$env:USERPROFILE\.android\debug.keystore" -alias androiddebugkey -storepass android -keypass android
```

انسخ SHA-1 من النتيجة وأضفه في:
Firebase Console → Project Settings → Your Apps → Android App → Add Fingerprint

#### ج. تفعيل Authentication Methods
في Firebase Console → Authentication → Sign-in method:
- ✅ تفعيل Email/Password
- ✅ تفعيل Google Sign-In

---

## 🚀 تشغيل التطبيق

### على Android
```bash
flutter run
```

### على Web
```bash
flutter run -d chrome
```

### تنظيف المشروع (إذا حدثت مشاكل)
```bash
flutter clean
flutter pub get
flutter run
```

---

## 📱 استخدام التطبيق

### للطلاب:
1. **التسجيل**:
   - أدخل الاسم الثلاثي (مثال: محمد أحمد علي)
   - أدخل البريد الإلكتروني وكلمة المرور
   - حدد المدرسة، الصف، والفصل
   - اضغط "إنشاء الحساب"

2. **تسجيل الدخول بـ Google**:
   - اختر "طالب"
   - أدخل المدرسة، الصف، والفصل في النافذة المنبثقة
   - اضغط "حفظ"

3. **نسيت كلمة المرور**:
   - أدخل البريد الإلكتروني
   - اضغط "نسيت كلمة المرور؟"
   - تحقق من بريدك الإلكتروني للحصول على رابط إعادة التعيين

### للمعلمين:
1. **التسجيل**: نفس خطوات الطالب (لا حاجة للمدرسة/الصف/الفصل)

2. **عرض بيانات الطلاب**:
   - اذهب إلى "إدارة الطلاب"
   - شاهد قائمة جميع الطلاب ببياناتهم الكاملة

3. **تصدير البيانات كـ Excel**:
   - في شاشة "إدارة الطلاب"
   - اضغط على أيقونة التنزيل 📥
   - سيتم حفظ الملف في مجلد التنزيلات

---

## 🔍 إصلاح المشاكل الشائعة

### 1. خطأ Firebase على الويب
**المشكلة**: `FirebaseException: type 'FirebaseException' is not a subtype of type 'JavaScriptObject'`

**الحل**:
- تأكد من إضافة Web App في Firebase Console
- حدّث `appId` في `firebase_options.dart` للويب

### 2. خطأ Google Sign-In
**المشكلة**: `The class 'GoogleSignIn' doesn't have an unnamed constructor`

**الحل**: ✅ تم إصلاحه - استخدام `GoogleSignIn.instance`

### 3. خطأ Gradle على Android
**المشكلة**: `Could not close incremental caches`

**الحل**:
```bash
flutter clean
flutter pub get
```

### 4. خطأ SHA-1
**المشكلة**: Google Sign-In لا يعمل على Android

**الحل**: أضف SHA-1 fingerprint في Firebase Console (راجع قسم الإعداد)

---

## 📦 المكتبات المستخدمة

```yaml
dependencies:
  flutter:
    sdk: flutter
  provider: ^6.1.5+1
  firebase_core: ^4.2.0
  firebase_auth: ^6.1.1
  cloud_firestore: ^6.0.3
  google_sign_in: ^7.2.0
  excel: ^4.0.6                    # جديد - للتصدير
  permission_handler: ^11.3.1      # جديد - للأذونات
  image_picker: ^1.1.2
  path_provider: ^2.1.2
  shared_preferences: ^2.5.3
  confetti: ^0.8.0
  intl: ^0.19.0
```

---

## 📄 الملفات المهمة

### تم إضافتها/تحديثها:
- ✅ `lib/services/excel_export_service.dart` - جديد
- ✅ `lib/screens/registration_screen.dart` - تم التحديث
- ✅ `lib/screens/login_screen.dart` - تم التحديث
- ✅ `lib/screens/student_management_screen.dart` - تم التحديث
- ✅ `lib/services/auth_service.dart` - تم التحديث
- ✅ `lib/services/user_provider.dart` - تم التحديث
- ✅ `lib/firebase_options.dart` - تم التحديث
- ✅ `android/app/src/main/AndroidManifest.xml` - تم التحديث (أذونات)
- ✅ `android/app/build.gradle.kts` - تم التحديث
- ✅ `android/settings.gradle.kts` - تم التحديث
- ✅ `pubspec.yaml` - تم التحديث

---

## ✅ قائمة التحقق النهائية

- [x] الاسم الثلاثي إلزامي في التسجيل
- [x] إضافة حقول المدرسة/الصف/الفصل للطلاب
- [x] حفظ البيانات في Profile
- [x] عرض البيانات الكاملة للمعلم
- [x] تصدير البيانات كـ Excel
- [x] إصلاح Google Sign-In
- [x] إضافة dialog للبيانات الإضافية عند Google Sign-In
- [x] إصلاح خطأ التسجيل (loading مستمر)
- [x] إضافة رسالة نجاح بعد التسجيل
- [x] إعادة تعيين كلمة المرور تعمل
- [x] إرسال إيميل لإعادة تعيين كلمة المرور

---

## 🎯 التوصيات التالية (اختياري)

1. **نشر التطبيق**: نشر على Google Play أو App Store
2. **إشعارات Push**: إضافة Firebase Cloud Messaging
3. **تحليلات**: إضافة Firebase Analytics
4. **نسخ احتياطي**: تصدير قاعدة البيانات بشكل دوري
5. **التقارير المتقدمة**: رسوم بيانية للأداء

---

## 💡 ملاحظات مهمة

- 🔐 **الأمان**: كلمة المرور يجب أن تكون 6 أحرف على الأقل
- 📧 **البريد الإلكتروني**: يجب أن يكون صالحاً للحصول على رابط إعادة التعيين
- 📱 **الأذونات**: التطبيق يطلب إذن التخزين لحفظ ملف Excel
- 🌐 **الإنترنت**: مطلوب اتصال بالإنترنت للمصادقة وحفظ البيانات

---

## 📞 الدعم

إذا واجهت أي مشاكل:
1. تحقق من قسم "إصلاح المشاكل الشائعة"
2. شغّل `flutter clean` و `flutter pub get`
3. تأكد من إعداد Firebase بشكل صحيح

---

**تم التحديث**: 31 أكتوبر 2025
**النسخة**: 1.0.0
